
# Pair Sum in BST

---

# 1. Problem Statement

Given a **Binary Search Tree** and an integer **X**, determine whether there exist **two distinct nodes** whose sum equals **X**.

Return

- **true** → Pair exists
- **false** → Pair doesn't exist

---

# 2. Pattern Recognition ⭐⭐⭐⭐⭐

|Item|Value|
|---|---|
|Data Structure|BST|
|Pattern|DFS + HashSet|
|Alternative Pattern|Inorder + Two Pointer|
|Advanced|Two BST Iterators|
|Difficulty|Medium|

---

# 3. Recognition Clues

Whenever you hear

- Pair Sum
- Two Sum
- Find two nodes
- Sum = X
- BST

Immediately think

```
Two Sum Problem
```

NOT

```
BST Search
```

This is the biggest interview trick.

---

# 4. Core Intuition ⭐⭐⭐⭐⭐

For every node,

suppose current value is

```
20
```

Need

```
X-20
```

If that value has already been visited,

Pair found.

Otherwise

Store

```
20
```

Continue traversal.

Exactly the same logic as **Two Sum using HashSet**.

The BST property is **not actually required** for this approach. The same idea also works for a normal Binary Tree.

---

# 5. Approach Evolution

## Approach 1

### Brute Force

Store all nodes.

Check every pair.

```
for every node

    for every node
```

Time

```
O(N²)
```

Space

```
O(N)
```

Never code this.

Only mention it.

---

# Approach 2 ⭐⭐⭐⭐⭐ (Most Interview Friendly)

## DFS / Inorder + HashSet (or TreeSet)

Idea

Traverse tree.

For every node

Need

```
target-current
```

If

```
HashSet contains

target-current
```

Return

```
true
```

Else

Store current value.

Continue traversal.

---

# Why Inorder?

Many candidates ask this.

Answer

> **It doesn't have to be inorder.**

You can use

- Preorder
- Inorder
- Postorder

because **HashSet lookup is independent of traversal order**.

GFG uses inorder simply because the problem belongs to the BST section.

This is an excellent interview point.

---

# Algorithm

```
Create HashSet

Traverse Tree

If node is null

return false

Visit Left

If target-current exists

return true

Insert current

Visit Right

If found anywhere

return true

Else

return false
```

---

# Code (Your GFG Solution)

```java
class GFG {

    static boolean findPair(Node root, int X) {

        TreeSet<Integer> set = new TreeSet<>();

        return inorder(root, X, set);
    }

    static boolean inorder(Node root,
                           int X,
                           TreeSet<Integer> set) {

        if (root == null)
            return false;

        // Search left subtree
        if (inorder(root.left, X, set))
            return true;

        // Check complement
        if (set.contains(X - root.data))
            return true;

        // Store current value
        set.add(root.data);

        // Search right subtree
        return inorder(root.right, X, set);
    }
}
```

---

# Dry Run ⭐⭐⭐⭐⭐

Tree

```
        15
      /    \
    10      20
   /  \    /  \
  8   12 16   25
```

Target

```
28
```

---

Traversal

Visit

```
8
```

Need

```
20
```

Not found.

Store

```
8
```

---

Visit

```
10
```

Need

```
18
```

Not found.

Store

```
10
```

---

Visit

```
12
```

Need

```
16
```

Not found.

Store

```
12
```

---

Visit

```
15
```

Need

```
13
```

Not found.

Store

```
15
```

---

Visit

```
16
```

Need

```
12
```

Already exists.

Return

```
true
```

---

# Recursion Tree

```
findPair(15)

↓

10

↓

8

↓

null

visit 8

↓

null

return

↓

visit 10

↓

12

↓

visit 12

↓

return

↓

visit 15

↓

20

↓

16

↓

visit 16

↓

Found
```

---

# Complexity ⭐⭐⭐⭐⭐

Time

```
O(N)
```

Every node visited once.

---

Space

```
O(N)
```

HashSet stores at most all node values.

Recursion stack

```
O(H)
```

Overall auxiliary space is commonly stated as **O(N)** because the set dominates.

---

# Why TreeSet Works?

GFG uses

```
TreeSet<Integer>
```

Could also use

```
HashSet<Integer>
```

### HashSet

Lookup

```
O(1)
```

Average

Preferred in interviews.

---

### TreeSet

Lookup

```
O(logN)
```

Implemented using a Red-Black Tree.

Works correctly.

Slightly slower.

---

Interview Answer

> "I'd use HashSet because it provides average O(1) lookup, while TreeSet gives O(log N) lookup and is only needed if sorted-order operations are required."

---

# Better BST Solution ⭐⭐⭐⭐

Inorder

↓

Sorted Array

↓

Two Pointer

Time

```
O(N)
```

Space

```
O(N)
```

Uses BST property.

---

# Best BST Solution ⭐⭐⭐⭐⭐

Two BST Iterators

```
Smallest

↓

Largest
```

Exactly like

```
Two Pointer
```

without creating an array.

Time

```
O(N)
```

Space

```
O(H)
```

This is usually the most optimized BST-specific solution.

---
# Approach 3 ⭐⭐⭐⭐⭐ (BST Property)

## Intuition

Unlike the HashSet approach, this solution **uses the BST property**.

### Observation

**Inorder traversal of a BST is always sorted.**

Example

```
        8
      /   \
     4     12
    / \    / \
   2   6 10 14
```

Inorder

```
2 4 6 8 10 12 14
```

Now the problem becomes

```
Pair Sum in Sorted Array
```

Once you have a sorted list, use the classic **Two Pointer** technique.

---

# Algorithm

### Step 1

Store inorder traversal in an ArrayList.

```
List

↓

2 4 6 8 10 12 14
```

---

### Step 2

```
left = 0;
right = list.size() - 1;
```

---

### Step 3

```
sum = list[left] + list[right]
```

If

```
sum == target
```

Return

```
true
```

If

```
sum < target
```

Move

```
left++
```

Else

```
right--
```

---

# Dry Run

Target

```
16
```

Array

```
2 4 6 8 10 12 14
```

Initially

```
L             R

2           14

Sum = 16

Found
```

---

Target

```
18
```

```
2 + 14 = 16

Need bigger

left++
```

```
4 + 14 = 18

Found
```

---

# Code

```java
class Solution {

    List<Integer> list = new ArrayList<>();

    void inorder(Node root) {

        if (root == null)
            return;

        inorder(root.left);

        list.add(root.data);

        inorder(root.right);
    }

    boolean findPair(Node root, int target) {

        inorder(root);

        int left = 0;
        int right = list.size() - 1;

        while (left < right) {

            int sum = list.get(left) + list.get(right);

            if (sum == target)
                return true;

            if (sum < target)
                left++;
            else
                right--;
        }

        return false;
    }
}
```

---

# Complexity

### Time

- Inorder Traversal → **O(N)**
- Two Pointer Scan → **O(N)**

**Overall:** **O(N)**

### Space

- ArrayList → **O(N)**
- Recursion Stack → **O(H)**

Overall auxiliary space is typically considered **O(N)** because the list dominates.

---

# When Should You Use This?

This is an excellent answer when the interviewer specifically wants you to **exploit the BST property**.

Compared to the HashSet approach:

### Advantages

- Clearly leverages the BST property.
- Uses the well-known two-pointer pattern on a sorted sequence.
- Easy to explain and implement.

### Disadvantages

- Requires storing the entire inorder traversal.
- Uses **O(N)** extra space.


---

# Similar Problems

- Two Sum
- Two Sum II (Sorted Array)
- Pair Sum in Binary Tree
- BST Iterator
- Two Sum IV - Input is a BST (LeetCode 653)

---



### Q1. Why use a Set?

To check whether the complement (`X - current`) has already been seen in constant time.

---

### Q2. Why use inorder?

For the HashSet approach, **it's not required**. Any traversal order works. Inorder is often chosen because the input is a BST.

---

### Q3. Can this work for a Binary Tree?

**Yes.** The HashSet solution is independent of BST ordering.

---

### Q4. Which Set would you choose?

**HashSet**, because average lookup is **O(1)**.

---

### Q5. Can you reduce space?

**Yes.** Use two BST iterators (forward and reverse inorder) to simulate two pointers with **O(H)** extra space.

---

# 30-Second Revision Box

```
Pattern
Two Sum + DFS

Observation
Need complement (X - current)

Brute Force
All pairs → O(N²)

Interview Solution
DFS + HashSet → O(N), O(N)

BST Better
Inorder + Two Pointer → O(N), O(N)

BST Best
Two BST Iterators → O(N), O(H)

Remember
Traversal order doesn't matter for HashSet.

HashSet > TreeSet for this problem.
```


|Approach|Time|Space|Interview Value|
|---|---|---|---|
|**1. Brute Force (Check every pair)**|O(N²)|O(N)|Mention only|
|**2. DFS + HashSet**|O(N)|O(N)|Most practical|
|**3. Inorder + ArrayList + Two Pointers**|O(N)|O(N)|Uses BST property|
|**4. Two BST Iterators**|O(N)|O(H)|Best BST-specific solution|

---

